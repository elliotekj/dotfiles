# - Plugins ----------------------------------------------------------

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'omerxx/tmux-floax'
set -g @plugin 'ddzero2c/tmux-easymotion'
set -g @plugin 'laktak/extrakto'

# extrakto settings
set -g @extrakto_key 'none'

# automatically install tmux plugin on new machines,
# see https://github.com/tmux-plugins/tpm/blob/master/docs/automatic_tpm_installation.md.
if "test ! -d ~/.config/tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.config/tmux/plugins/tpm && ~/.config/tmux/plugins/tpm/bin/install_plugins'"

set-option -g default-shell /bin/zsh

# - Appearance -------------------------------------------------------

set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"
set -g cursor-style blinking-bar

# FZF Rose Pine theme (for popup menus via run-shell)
set-environment -g FZF_DEFAULT_OPTS "--color=fg:#908caa,bg:#191724,hl:#ebbcba --color=fg+:#e0def4,bg+:#26233a,hl+:#ebbcba --color=info:#9ccfd8,prompt:#c4a7e7,pointer:#c4a7e7 --color=marker:#a3be8c,spinner:#f6c177,header:#31748f --color=border:#6e6a86"

set -g status on
set -g status-position top
set -g status-interval 5
set -g mouse on
# Clicking a pane selects it without entering copy mode.
# Text selection still works via click-drag.
bind -n MouseDown1Pane select-pane -t =

# Status bar styling (Rose Pine)
set -g status-style 'bg=#191724,fg=#e0def4'
set -g status-left '#([ "$(tmux list-clients | wc -l | tr -d " ")" -gt 1 ] && echo "#[bg=#eb6f92,fg=#191724,bold] ⚠ SSH SESSION ⚠ #[default] ")'
set -g status-left-length 30
set -g status-right '#{?window_zoomed_flag, [ZOOMED] ,}#(~/bin/tmux_sessions.sh)'
set -g status-right-length 100
set -g status-justify left

set -g window-status-format '#[fg=#908caa] #W '
set -g window-status-current-format '#[bg=#ebbcba fg=#191724 bold] #W '
set -g window-status-separator ''

# Pane styling (no dimming)
set -g window-style 'fg=#e0def4,bg=#191724'
set -g window-active-style 'fg=#e0def4,bg=#191724'
set -g display-panes-time 5000
set -g pane-base-index 1

# Pane border styling
set -g pane-border-style 'fg=#524f67,bg=#191724'
set -g pane-active-border-style 'fg=#524f67,bg=#191724'

# - Mappings ---------------------------------------------------------

# easy motion
set -g @easymotion-key 'Space'

# better copy/paste
setw -g mode-keys vi
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "pbcopy"
bind P paste-buffer
bind -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe "pbcopy"

unbind-key s
bind s split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"

# choose a session from the session list
bind S choose-session

# rename session
bind < command-prompt -I "#S" "rename-session '%%'"

# refresh config
bind-key R source-file ~/.config/tmux/tmux.conf

# window creation
# bind c display-popup -w 80% -h 80% -e SHELL=/bin/sh -E ~/bin/tmux_yazi_new_window.sh
bind c new-window -a

# skip "kill-pane 1? (y/n)" prompt
bind-key x kill-pane

# move current pane to its own window
bind b break-pane

# switch to another session when current session is destroyed
set -g detach-on-destroy off


# move around windows
bind -n 'M-{' previous-window
bind -n 'M-}' next-window

# reorder windows (move tab left/right)
bind -n 'M-<' 'swap-window -t -1; select-window -t -1'
bind -n 'M->' 'swap-window -t +1; select-window -t +1'

# move around panes
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R
bind -n M-h select-pane -L
bind -n M-j select-pane -D
bind -n M-k select-pane -U
bind -n M-l select-pane -R

# resize panes
bind - resize-pane -D 10
bind + resize-pane -U 10

bind -n C-k display-popup -w 70% -h 50% -E "~/bin/tmux_window_switch.sh"
bind -n C-p run-shell -b "~/bin/tmux_commands.sh"

set -g @floax-bind '-n M-t'
set -g @floax-change-path 'false'

# TMUX plugin manager (keep at the bottom of tmux.conf)
run -b '~/.config/tmux/plugins/tpm/tpm'
